use gaps_rs::opt;
use gaps_rs::Stats;

fn main() {
    println!("Hello World!");
}

// runs gaps, nwk and rfdist binaries and returns a stats struct (with valid rfdist)
fn get_stats(opt: opt::Gaps) -> Stats {
    unimplemented!()
}